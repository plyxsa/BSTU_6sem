<UserControl x:Class="PlushFood.Views.UserControls.ReturnTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlushFood.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <DataGrid Name="ReturnsDataGrid" AutoGenerateColumns="False"
                  BorderBrush="Transparent"
                  ItemsSource="{Binding Returns}"
                  SelectedItem="{Binding SelectedReturn, Mode=TwoWay}"
                  CanUserAddRows="False"
                  Foreground="{DynamicResource SecondaryBrush}"
                  RowBackground="{DynamicResource PrimaryBrush}"
                  Background="{DynamicResource PrimaryBrush}"
                  IsReadOnly="True">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Foreground" Value="{DynamicResource SecondaryBrush}" />
                    <Setter Property="Background" Value="{DynamicResource ThirdBrush}" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID возврата" Binding="{Binding ReturnID}" Width="50"/>
                <DataGridTextColumn Header="ID заказа" Binding="{Binding OrderID}" Width="100"/>
                <DataGridTextColumn Header="Причина" Binding="{Binding Reason}" Width="*"/>
                <DataGridTextColumn Header="Дата возврата" Binding="{Binding ReturnDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="150"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
