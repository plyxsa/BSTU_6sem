<UserControl x:Class="PlushFood.Views.UserControls.MealTableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlushFood.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <DataGrid Name="MealDataGrid" AutoGenerateColumns="False"
                  BorderBrush="Transparent"
                  ItemsSource="{Binding MealsWithDescription}"
                  SelectedItem="{Binding SelectedMeal, Mode=TwoWay}" 
                  CanUserAddRows="False"
                  Foreground="{DynamicResource SecondaryBrush}"
                  RowBackground="{DynamicResource PrimaryBrush}"
                  Background="{DynamicResource PrimaryBrush}"
                  IsReadOnly="True">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Foreground" Value="{DynamicResource SecondaryBrush}" />
                    <Setter Property="Background" Value="{DynamicResource ThirdBrush}" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding MealID}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="*"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="*"/>
                <DataGridCheckBoxColumn Header="Доступность" Binding="{Binding IsAvailable}" Width="*"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Калории" Binding="{Binding Calories}" Width="*"/>
                <DataGridTextColumn Header="Белки" Binding="{Binding Proteins}" Width="*"/>
                <DataGridTextColumn Header="Жиры" Binding="{Binding Fats}" Width="*"/>
                <DataGridTextColumn Header="Углеводы" Binding="{Binding Carbohydrates}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
