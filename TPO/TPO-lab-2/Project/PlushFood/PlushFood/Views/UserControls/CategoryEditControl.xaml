<UserControl x:Class="PlushFood.Views.UserControls.CategoryEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlushFood.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Название категории -->
        <TextBlock Text="Название категории" Grid.Row="0" Grid.Column="0" Margin="0,5,0,0"
                   VerticalAlignment="Center" Foreground="{DynamicResource SecondaryBrush}"/>
        <TextBox Text="{Binding SelectedCategory.CategoryName, UpdateSourceTrigger=PropertyChanged}" 
                 VerticalAlignment="Center"
                 Grid.Row="0" Grid.Column="1" Margin="5" Height="30"/>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Сохранить" Margin="5" Command="{Binding SaveCategoryCommand}" Width="100" Height="35" />
            <Button Content="Удалить" Margin="5" Command="{Binding DeleteCategoryCommand}" Width="100" Height="35" />
            <Button Content="Создать" Margin="5" Command="{Binding CreateCategoryCommand}" Width="100" Height="35" />
        </StackPanel>
    </Grid>
</UserControl>
