<UserControl x:Class="PlushFood.Views.UserControls.AdminEditForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Имя пользователя -->
        <TextBlock Text="Имя пользователя" Grid.Row="0" Grid.Column="0" Margin="0,5,0,0"
                   VerticalAlignment="Center" Foreground="{DynamicResource SecondaryBrush}"/>
        <TextBox Text="{Binding SelectedAdmin.UserName, Mode=TwoWay}" 
                 VerticalAlignment="Center"
                 Grid.Row="0" Grid.Column="1" Margin="5" Height="30"/>

        <!-- Email -->
        <TextBlock Text="Email" Grid.Row="1" Grid.Column="0" Margin="0,5,0,0"
                   VerticalAlignment="Center" Foreground="{DynamicResource SecondaryBrush}"/>
        <TextBox Text="{Binding SelectedAdmin.Email, Mode=TwoWay}"
                 VerticalAlignment="Center"
                 Grid.Row="1" Grid.Column="1" Margin="5" Height="30"/>

        <!-- Пароль -->
        <TextBlock Text="Пароль" Grid.Row="2" Grid.Column="0" Margin="0,5,0,0"
           VerticalAlignment="Center" Foreground="{DynamicResource SecondaryBrush}"/>
        <PasswordBox x:Name="PasswordBox" 
             VerticalAlignment="Center"
             Grid.Row="2" Grid.Column="1" 
             Margin="5" Height="30"
             IsEnabled="{Binding IsAddingNewAdmin}"
             PasswordChanged="PasswordBox_PasswordChanged"/>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Сохранить" Margin="5" Command="{Binding SaveCommand}" Width="100" Height="35" />
            <Button Content="Удалить" Margin="5" Command="{Binding DeleteCommand}" Width="100" Height="35" />
            <Button Content="Создать нового" Margin="5" Command="{Binding CreateNewCommand}" Width="120" Height="35" />
        </StackPanel>
    </Grid>
</UserControl>