<UserControl x:Class="PlushFood.Views.UserControls.AdminsTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10" >
        <DataGrid Name="AdminsDataGrid" AutoGenerateColumns="False" 
                  BorderBrush="{DynamicResource PrimaryBrush}"
                  ItemsSource="{Binding Admins}" 
                  SelectedItem="{Binding SelectedAdmin, Mode=TwoWay}" 
                  CanUserAddRows="False"
                  Foreground="{DynamicResource SecondaryBrush}"
                  RowBackground="{DynamicResource PrimaryBrush}"
                  Background="{DynamicResource PrimaryBrush}"
                  IsReadOnly="True">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Foreground" Value="{DynamicResource SecondaryBrush}" />
                    <Setter Property="Background" Value="{DynamicResource ThirdBrush}" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding AdminID}" Width="50"/>
                <DataGridTextColumn Header="Имя пользователя" Binding="{Binding UserName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
