<Window x:Class="PlushFood.Views.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlushFood.Views"
        xmlns:viewmodels="clr-namespace:PlushFood.ViewModels"
        mc:Ignorable="d"
        Title="AdminView" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Grid>
        <Grid.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="1" Opacity="0.4"/>
        </Grid.Effect>
        <Border Background="{DynamicResource MainBrush}" CornerRadius="10" BorderBrush="Transparent">
            <Grid>
                <!-- Верхняя строка с кнопками -->
                <Grid Margin="10" VerticalAlignment="Top" Height="50" Background="{DynamicResource MainBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Кнопка Выйти -->
                    <Button Content="Выйти" Width="100" Height="40" Grid.Column="0" 
                            HorizontalAlignment="Left" VerticalAlignment="Center"
                            BorderBrush="Transparent" FontSize="16"
                            Command="{Binding LogoutCommand}"
                            Background="Transparent" Foreground="{DynamicResource SecondaryBrush}"/>

                    <!-- Кнопка Профиль -->
                    <Button Content="Профиль" Width="100" Height="40" Grid.Column="2" 
                            HorizontalAlignment="Right" VerticalAlignment="Center" 
                            BorderBrush="Transparent" FontSize="16"
                            Background="Transparent" 
                            Command="{Binding NavigateToProfileCommand}"
                            Foreground="{DynamicResource SecondaryBrush}"/>

                    <Button Content="◑" FontSize="28" Command="{Binding SwitchThemeCommand}"
                            HorizontalAlignment="Left" Width="40" Margin="615,0,0,0"
                            Background="Transparent" BorderBrush="Transparent"
                            BorderThickness="0" Height="40" VerticalAlignment="Center"
                            Foreground="{DynamicResource SecondaryBrush}" Grid.Column="1"
                            />
                </Grid>

                <!-- Вторая строка с разделами -->
                <Grid Background="{DynamicResource SecondBrush}" Margin="10,70,10,0" VerticalAlignment="Top" Height="50">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Админы" Grid.Column="0" HorizontalAlignment="Center" 
                            VerticalAlignment="Center" Width="70"
                            Background="{DynamicResource ThirdBrush}" FontSize="14"
                            BorderBrush="Transparent" Height="30"
                            Command="{Binding ShowAdminsCommand}"/>
                    <Button Content="Клиенты" Grid.Column="1" HorizontalAlignment="Center"
                            VerticalAlignment="Center" Width="70"
                            Background="{DynamicResource ThirdBrush}" FontSize="14"
                            BorderBrush="Transparent" Height="30"
                            Command="{Binding ShowClientsCommand}"/>
                    <Button Content="Категории" Grid.Column="2"
                            HorizontalAlignment="Center" VerticalAlignment="Center" Width="70"
                            Background="{DynamicResource ThirdBrush}" FontSize="14"
                            BorderBrush="Transparent" Height="30"
                            Command="{Binding ShowCategoriesCommand}" />
                    <Button Content="Блюда" Grid.Column="3" HorizontalAlignment="Center"
                            VerticalAlignment="Center"  Width="70"
                            Background="{DynamicResource ThirdBrush}" FontSize="14"
                            BorderBrush="Transparent" Height="30"
                            Command="{Binding ShowMealsCommand}"/>
                    <Button Content="Заказы" Grid.Column="4" HorizontalAlignment="Center"
                            VerticalAlignment="Center"  Width="70"
                            Background="{DynamicResource ThirdBrush}" FontSize="14"
                            BorderBrush="Transparent" Height="30"
                            Command="{Binding ShowOrdersCommand}"/>
                    <Button Content="Возвраты" Grid.Column="5" HorizontalAlignment="Center"
                            VerticalAlignment="Center"  Width="70"
                            Background="{DynamicResource ThirdBrush}" FontSize="14"
                            BorderBrush="Transparent" Height="30"
                            Command="{Binding ShowReturnsCommand}"/>
                </Grid>

                <!-- Основная область -->
                <Grid Margin="10,130,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Левая часть -->
                    <Border Grid.Column="0" Background="{DynamicResource PrimaryBrush}" CornerRadius="10" Margin="5">
                        <ContentControl x:Name="LeftContentControl" Content="{Binding LeftContent}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    </Border>

                    <!-- Правая часть -->
                    <Border Grid.Column="1" Background="{DynamicResource PrimaryBrush}" CornerRadius="10" Margin="5">
                        <ContentControl x:Name="RightContentControl" Content="{Binding RightContent}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    </Border>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</Window>